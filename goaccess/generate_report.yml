---
- name: Run GoAccess on all Caddy access logs
  hosts: all
  become: true
  tasks:
    - name: Generate combined GoAccess report from all subdomain logs
      ansible.builtin.shell: |
        goaccess /var/log/caddy/*/access.json -o /var/lib/caddy-stats/goaccess.html --log-format=CADDY
      changed_when: false
